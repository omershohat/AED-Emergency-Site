# NEXT_PUBLIC_ prefixed variables are the only ones the browser can read.
# These are just URLs - nothing secret ever belongs in a NEXT_PUBLIC_ variable.
NEXT_PUBLIC_API_URL=http://localhost:4000
NEXT_PUBLIC_MESH_URL=http://localhost:5000
NEXT_PUBLIC_MESH_WS=ws://localhost:5000
